{
  "name": "swiggy-clone-sample",
  "version": "0.1.0",
  "description": "Minimal Swiggy-clone backend for CI/CD practice",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "test": "jest --runInBand",
    "lint": "echo \"No linter configured; add ESLint if desired\" && exit 0",
    "build": "echo 'no build step'"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.6.1",
    "supertest": "^6.3.3"
  }
}