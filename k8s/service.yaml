apiVersion: v1
kind: Service
metadata:
  name: swiggy-svc
  namespace: swiggy
spec:
  type: NodePort
  selector:
    app: swiggy
  ports:
    - protocol: TCP
      port: 80         # service port
      targetPort: 80   # container port
      nodePort: 30001  # node port to reach from host
